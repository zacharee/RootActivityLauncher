<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:parentTag="android.widget.LinearLayout"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/clicker"
        android:layout_width="match_parent"
        android:layout_height="?listPreferredItemHeightSmall"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:focusable="true"
        android:clickable="true"
        android:background="?selectableItemBackground">

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:textColor="?android:textColorPrimary"
            android:textSize="18sp"
            android:gravity="center_vertical"
            android:text="@string/services"
            />

        <ImageView
            android:id="@+id/arrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_baseline_keyboard_arrow_down_24"
            android:contentDescription="@null"
            />

    </LinearLayout>

    <tk.zwander.rootactivitylauncher.views.ShadowFadingEdgeRecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:scrollbars="vertical"
        android:scrollbarThumbVertical="@drawable/scroll_thumb"
        android:requiresFadingEdge="vertical"
        android:fadingEdgeLength="@dimen/item_recycler_fading_edge_length"
        app:layoutManager="tk.zwander.rootactivitylauncher.util.PredictiveLinearLayoutManager"
        android:nestedScrollingEnabled="true"
        tools:listitem="@layout/component_item"
        tools:visibility="visible"
        tools:ignore="RtlSymmetry"
        />

</merge>